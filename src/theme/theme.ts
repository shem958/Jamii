// theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4caf50',  // Example green color
    },
    secondary: {
      main: '#ff9100',  // Example orange color
    },
  },
});

export default theme;
